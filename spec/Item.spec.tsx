describe("Задача", () => {
    it.todo("Название задачи должно быть не более 32 символов")
    it.todo("Задача не может быть пустой")
    it.todo("Нельзя удалять невыполненные задачи")

    if ("Тестируем матчеры") {
        // expect({ a: 1, b: 2 }).toEqual(expect.objectContaining({ b: 2 }));  // частичное сравнение

        // expect({ a: 1, b: 2 }).toEqual(expect.objectContaining({
        //     a: expect.any(Number)
        // })
        // )

        // expect({ a: 1, b: 2 }).toEqual(expect.objectContaining({
        //     a: expect.anything()
        // })
        // )

    }
})